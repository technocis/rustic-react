import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";

import { Card } from "./Card";

<Meta title="Card" component={Card} />

export const Template = (args) => <Card {...args}>{args.children}</Card>;

# Card

This is the `RusticUI` Card Component. It is fairly simple. You just wrap whatever content you want inside of `<Card>` and `</Card>`.

<Canvas>
  <Story
    name="Card"
    args={{
      children: <h1>Example Card</h1>,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
